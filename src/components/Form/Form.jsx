import React, {useState} from 'react'import styles from './Form.module.scss'export const Form = ({                         handleChangeName,                         handleChangeSizeX,                         handleChangeSizeY,                         handleChangeColor,                         handleChangeBarcode,                         handleChangeDate,                         date,                         name,                         color,                         sizeX,                         sizeY,                         barcode                     }) => {    const [isOpen, setIsOpen] = useState(true)    const handleCloseForm = () => {        setIsOpen(!isOpen)    }    return (        <div className={`${styles.wrapper} ${!isOpen ? styles.hide : ''}`}>            <form className={styles.appHeader}>                <button type={'button'} className={styles.close} onClick={handleCloseForm}>X</button>                <label>                    Chose your xls or xlsx:                    <input                        type="file"                        id="fileInput"                        accept="application/vnd.ms-excel, application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"                        disabled={!date || !name || !barcode || !sizeX || !sizeY || !color}                    />                </label>                <label>                    Name:                    <input                        type="text"                        onChange={handleChangeName}                        value={name}                    />                </label>                <label>                    Barcode:                    <input                        type="number"                        onChange={handleChangeBarcode}                        value={barcode}                    />                </label>                <label>                    Size:                    <input                        type="number"                        onChange={handleChangeSizeX}                        value={sizeX}                        min={1}                    />                    X                    <input                        type="number"                        onChange={handleChangeSizeY}                        value={sizeY}                        min={1}                    />                </label>                <label>                    Color:                    <input                        type="text"                        onChange={handleChangeColor}                        value={color}                    />                </label>                <label>                    Date of made:                    <input                        type="date"                        onChange={handleChangeDate}                        value={date}                    />                </label>            </form>        </div>    )}