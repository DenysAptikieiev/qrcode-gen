import React from 'react'import styles from './Form.module.scss'export const Form = ({                         handleChangeBarcode,                         handleChangeDate,                         isOpenForm,                         date,                         barcode                     }) => {    return (        <div className={`${styles.wrapper} ${!isOpenForm ? styles.hide : ''}`}>            <form className={styles.appHeader}>                <label>                    Barcode:                    <input                        type="number"                        onChange={handleChangeBarcode}                        value={barcode}                    />                </label>                <label>                    Date of made:                    <input                        type="date"                        onChange={handleChangeDate}                        value={date}                    />                </label>                <label htmlFor="fileInput" className={styles.fileUpload}>                    Upload File                    <input                        id="fileInput"                        type="file"                        accept="application/vnd.ms-excel, application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"                        disabled={!date || !barcode}                    />                    {!date || !barcode ?                        <span className={styles.errorMessage}>Please enter date and barcode</span> : null}                </label>            </form>        </div>    )}